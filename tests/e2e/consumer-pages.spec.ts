import { expect,test } from "@playwright/test";const pages=[["/personal","Everyday money, made simpler."],["/features","Everything COOV brings together."],["/savings","Save for what matters."],["/bills-and-payments","Pay the everyday things from one place."]] as const;for(const [path,heading] of pages)test(`${path} renders its primary message without overflow`,async({page})=>{await page.setViewportSize({width:390,height:900});await page.goto(path);await expect(page.getByRole("heading",{level:1})).toHaveText(heading);expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false)});

import { expect,test } from "@playwright/test";test("download page has safe coming soon controls",async({page})=>{await page.goto("/download");await expect(page.getByText("Coming Soon").first()).toBeVisible();const bad=await page.locator('a[href=""],a[href="#"]').count();expect(bad).toBe(0)});for(const route of ["/help","/legal/privacy","/legal/terms","/legal/cookies"])test(`${route} responds`,async({page})=>{const res=await page.goto(route);expect(res?.ok()).toBe(true)});

import { downloadLinks } from "@/config/download-links";import { resolveStoreLink } from "@/lib/store-links";
function StoreControl({label,url}:{label:string;url:string}){const state=resolveStoreLink(url);if(state.status==="available")return <a className="inline-flex min-h-12 items-center rounded-full bg-[#F7F4EF] px-5 text-sm font-semibold text-[#160D20]" href={state.href} target="_blank" rel="noreferrer">{label}</a>;return <button className="inline-flex min-h-12 cursor-not-allowed items-center rounded-full border border-white/20 bg-white/5 px-5 text-sm font-semibold text-white/70" disabled aria-disabled="true">{label} · Coming Soon</button>}
export function AppStoreButtons(){return <div className="flex flex-wrap gap-3"><StoreControl label="App Store" url={downloadLinks.ios}/><StoreControl label="Google Play" url={downloadLinks.android}/></div>}

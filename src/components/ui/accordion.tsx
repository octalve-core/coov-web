"use client";
import { useId,useState } from "react";
export function Accordion({items}:{items:readonly {question:string;answer:string}[]}){const base=useId();const [open,setOpen]=useState<number|null>(0);if(!items.length)return <p className="text-white/60">More help content is coming soon.</p>;return <div className="divide-y divide-white/10">{items.map((item,index)=>{const active=open===index;const panel=`${base}-panel-${index}`;return <div key={item.question} className="py-2"><button aria-expanded={active} aria-controls={panel} className="flex w-full items-center justify-between gap-6 py-5 text-left font-semibold" onClick={()=>setOpen(active?null:index)}><span>{item.question}</span><span aria-hidden>{active?"−":"+"}</span></button><div id={panel} hidden={!active} className="pb-5 pr-10 text-sm leading-7 text-white/65">{item.answer}</div></div>})}</div>}

import { ContentSection } from "@/components/shared/content-section";
export function AuthenticationSection(){return <ContentSection eyebrow="Authentication" title="Protect account access." body="Authentication controls are designed to reduce casual account exposure." items={['Protected sessions','Step-up checks']} light={false}/>}

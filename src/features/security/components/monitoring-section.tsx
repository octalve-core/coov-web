import { ContentSection } from "@/components/shared/content-section";
export function MonitoringSection(){return <ContentSection eyebrow="Monitoring" title="Keep an audit trail." body="Monitoring and audit events support operational review and compliance workflows." items={['Audit events','Transaction monitoring']} light={true}/>}

import { ContentSection } from "@/components/shared/content-section";
export function DataHandlingSection(){return <ContentSection eyebrow="Data handling" title="Treat sensitive information carefully." body="The platform architecture is designed around controlled access to sensitive identity and transaction data." items={['Access control','Secrets handling']} light={false}/>}

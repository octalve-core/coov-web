import { ContentSection } from "@/components/shared/content-section";
export function IdentitySection(){return <ContentSection eyebrow="Identity" title="Know who is using the account." body="Identity verification forms part of account setup and capability access." items={['Phone verification','BVN/NIN flows','Selfie/facial checks']} light={true}/>}

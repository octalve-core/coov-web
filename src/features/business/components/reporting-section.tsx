import { ContentSection } from "@/components/shared/content-section";
export function ReportingSection(){return <ContentSection eyebrow="Reporting" title="Understand activity over time." body="Reporting features are described at capability level until production details are final." items={['Transaction records','Export-ready thinking']} light={false}/>}

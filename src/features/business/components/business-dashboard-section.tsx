import { ContentSection } from "@/components/shared/content-section";
export function BusinessDashboardSection(){return <ContentSection eyebrow="Dashboard" title="See business activity in one view." body="A focused business dashboard helps surface the information operators need." items={['Recent payments','Status visibility','Operational view']} light={true}/>}

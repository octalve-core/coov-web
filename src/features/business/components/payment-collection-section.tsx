import { ContentSection } from "@/components/shared/content-section";
export function PaymentCollectionSection(){return <ContentSection eyebrow="Collections" title="Make getting paid easier to follow." body="Give businesses a clear place to receive and review incoming payment activity." items={['Payment collection','Transaction activity']} light={true}/>}

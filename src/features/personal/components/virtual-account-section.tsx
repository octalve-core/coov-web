import { ContentSection } from "@/components/shared/content-section";
export function VirtualAccountSection(){return <ContentSection eyebrow="Receive money" title="An account experience for receiving value." body="Keep receiving details simple and easy to share." items={['Account details','Receive flows']} light={true}/>}

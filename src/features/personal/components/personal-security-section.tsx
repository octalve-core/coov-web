import { ContentSection } from "@/components/shared/content-section";
export function PersonalSecuritySection(){return <ContentSection eyebrow="Security" title="Protection around everyday activity." body="Identity, authentication and transaction controls are considered as part of the product experience." items={['Identity checks','Protected access','Transaction monitoring']} light={false}/>}

import { ContentSection } from "@/components/shared/content-section";
export function TransfersSection(){return <ContentSection eyebrow="Transfers" title="Move value with fewer steps." body="Send to COOV users or bank accounts through guided transaction flows." items={['Recipient details','Amount review','Transaction confirmation','Clear status']} light={false}/>}

import { ContentSection } from "@/components/shared/content-section";
export function CategoriesSection(){return <ContentSection eyebrow="Categories" title="Common payments, one flow." body="COOV plans support everyday categories without claiming providers that are not yet confirmed." items={['Airtime','Data','Electricity','TV subscriptions']} light={true}/>}

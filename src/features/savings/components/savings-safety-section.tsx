import { ContentSection } from "@/components/shared/content-section";
export function SavingsSafetySection(){return <ContentSection eyebrow="Responsible presentation" title="No invented yield claims." body="COOV will publish product-specific rates or terms only when they are verified and ready." items={['Clear terms','Product-specific disclosures']} light={true}/>}

import { makeMetadata } from "@/lib/metadata";
export const metadata=makeMetadata("Savings","Create room for goals and plans with the COOV savings experience.");
import Page from "@/features/savings/page";export default function Route(){return <Page/>}

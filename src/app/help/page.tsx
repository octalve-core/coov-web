import { makeMetadata } from "@/lib/metadata";
export const metadata=makeMetadata("Help","Find answers about COOV, product availability, and support.");
import HelpPage from "@/features/help/page";export default function Page(){return <HelpPage/>}

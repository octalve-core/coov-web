import { makeMetadata } from "@/lib/metadata";
export const metadata=makeMetadata("Download","Get the official COOV app links when the production application is released.");
import DownloadPage from "@/features/download/page";export default function Page(){return <DownloadPage/>}

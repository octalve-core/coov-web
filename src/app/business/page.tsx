import { makeMetadata } from "@/lib/metadata";
export const metadata=makeMetadata("Business","Payment tools designed to keep business moving with COOV.");
import Page from "@/features/business/page";export default function Route(){return <Page/>}

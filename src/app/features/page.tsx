import { makeMetadata } from "@/lib/metadata";
export const metadata=makeMetadata("Features","Explore transfers, payments, savings, business tools, and security across COOV.");
import Page from "@/features/features/page";export default function Route(){return <Page/>}

import { makeMetadata } from "@/lib/metadata";
export const metadata=makeMetadata("Bills & Payments","Pay airtime, data, electricity, TV subscriptions, and other supported everyday bills with COOV.");
import Page from "@/features/bills-and-payments/page";export default function Route(){return <Page/>}

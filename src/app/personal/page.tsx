import { makeMetadata } from "@/lib/metadata";
export const metadata=makeMetadata("Personal","Everyday money, made simpler with COOV.");
import Page from "@/features/personal/page";export default function Route(){return <Page/>}

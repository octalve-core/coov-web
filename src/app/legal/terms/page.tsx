import { makeMetadata } from "@/lib/metadata";
export const metadata=makeMetadata("Terms","Read the pre-launch COOV website terms.");
import TermsPage from "@/features/legal/terms";export default function Page(){return <TermsPage/>}

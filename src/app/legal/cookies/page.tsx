import { makeMetadata } from "@/lib/metadata";
export const metadata=makeMetadata("Cookies","Read the pre-launch COOV cookie notice.");
import CookiesPage from "@/features/legal/cookies";export default function Page(){return <CookiesPage/>}
